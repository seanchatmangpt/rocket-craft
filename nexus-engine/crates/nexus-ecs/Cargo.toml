[package]
name = "nexus-ecs"
version = "0.1.0"
edition = "2021"

[dependencies]
hecs = { version = "0.10" }
serde = { version = "1", features = ["derive"] }
thiserror = "1"
rand = { version = "0.8", features = ["small_rng"] }

[dev-dependencies]
proptest = "1"
