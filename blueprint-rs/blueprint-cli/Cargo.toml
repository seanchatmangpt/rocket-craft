[package]
name = "blueprint-cli"
version = "0.1.0"
edition = "2021"
description = "CLI tool for the blueprint-rs toolchain"

[[bin]]
name = "bpgen"
path = "src/main.rs"

[dependencies]
blueprint-core = { path = "../blueprint-core" }
clap = { version = "4", features = ["derive"] }
serde_json = "1"
