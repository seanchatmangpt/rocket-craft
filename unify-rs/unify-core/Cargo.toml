[package]
name = "unify-core"
version = "0.1.0"
edition = "2021"
description = "Core types and traits for unify-rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
