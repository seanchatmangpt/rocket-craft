# The BIG BANG $\mu$-Pipeline: Branchless Typestates & Zero-Sized Types (ZST)

**Iteration:** 15 (Cron Iteration 10)
**Target Ecosystem:** `~/ggen` (Core Manufacturing Pipeline)
**Source Files Analyzed:** 
- `crates/ggen-core/src/manufacturing/gates.rs`
- `crates/ggen-core/src/manufacturing/operator.rs`

## Prologue
The 5-minute schedule loop has executed. The user's AGENTS.md document declares the law of "Branchless Typestates: Eliminate Runtime Branching. Structural and mathematical constraints must be pushed into the compiler type system." This iteration uncovers exactly how `ggen` implements this physical law using Rust's Zero-Sized Types (ZST) to constrain the AGI at compile time.

## First Principles Extracted: Compiler-Level Guardrails

### 1. Typestate Enforcements (`gates.rs`)
To prevent the AGI from hallucinating incorrect object lifecycles (e.g., trying to execute an artifact before it is admitted, or executing it twice), `ggen` completely removes the concept of `if (status == "pending")` runtime checks. 

Instead, it uses Rust's type system to physically model state transitions:
```rust
pub struct Pending;
pub struct Hot;
pub struct Spent;

pub struct UMotion<State> {
    pub payload: String,
    _state: PhantomData<State>,
}
```
In this model, an action (`UMotion`) starts as `UMotion<Pending>`. 
- The `.admit()` function is *only* implemented for `UMotion<Pending>`, and it consumes the struct to return a `UMotion<Hot>`.
- The `.execute()` function is *only* implemented for `UMotion<Hot>`, and it consumes the struct to return a `UMotion<Spent>`.

### 2. The Physical Impossibility of Hallucination
Because these states are evaluated by the Rust compiler, if the AGI attempts to write code that executes a `Pending` artifact:
`let spent = pending.execute();`
The Rust compiler will throw a fatal error. The code will physically fail to compile. The AGI cannot bypass this with a mock or a fake runtime test. It must obey the mathematical progression of states (`Pending -> Hot -> Spent`).

## Conclusion
The BIG BANG pipeline does not rely on unit tests to ensure the AGI uses correct object lifecycles. It uses the Rust compiler's type system as a physical barrier. By encoding domain logic into Zero-Sized Types, it guarantees that any code generated by the AGI that violates the ontology's state machine will be utterly rejected by the native toolchain (`part_compiler.rs`) before it can ever be executed. This is the definition of Branchless Authority.
