[package]
name = "wasm-tests"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
wasm-core = { path = "../wasm-core" }
wasm-bindgen = { workspace = true }
serde = { workspace = true }
