[package]
name = "wasm-tests"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
wasm-core = { path = "../wasm-core" }
wasm-game-logic = { path = "../wasm-game-logic" }
wasm-ui = { path = "../wasm-ui" }
wasm-patterns = { path = "../wasm-patterns" }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
chicago-tdd-tools = { path = "../../chicago-tdd-tools" }
