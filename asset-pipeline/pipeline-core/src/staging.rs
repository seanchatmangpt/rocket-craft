// TODO: Implemented by Agent 3 — FBX copy-into-content-dir + manifest writer.
// Responsible for: atomic copy of FBX to output_dir, writing/updating a
// JSON manifest (path → hash), and advancing ConvertedAsset → StagedAsset.
