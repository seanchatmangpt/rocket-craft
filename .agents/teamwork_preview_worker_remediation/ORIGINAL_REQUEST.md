## 2026-06-19T19:37:07Z
You are the Remediation Worker (teamwork_preview_worker).
Your working directory is `/Users/sac/rocket-craft/.agents/teamwork_preview_worker_remediation/`.

You must resolve all integrity violations identified by the Forensic Auditor in `crates/mech_factory_mud` and ensure the implementation is 100% genuine and robust.

Please perform the following remediation steps:

1. **Re-implement MUD CLI commands** in `crates/mech_factory_mud/src/main.rs` with genuine logic instead of hardcoded print statements:
   - `simulate`: Execute a real simulation with the given scenario name (using `Simulation::run(&scenario)`) and write the trace, ocel, receipts, projection manifest, and report JSON/MD files.
   - `verify`: Load the simulation files (`trace.json`, `ocel.json`, `receipts.jsonl`, `projection_manifest.json`) from `generated/mech_factory_mud/` and perform actual verification (check files exist, call `verify_receipt_chain` to check receipts, verify `FactoryStations.csv` and `WalkthroughRoute.csv` exist). Print `PASS` on success.
   - `replay`: Load the receipts from `factory_walkthrough.receipts.jsonl` and run `verify_receipt_chain`. Print `Replay passed.` or `PASS`.
   - `export-ue4`: Load the projection manifest from the simulation, run `export_ue4` by copying the actual, schema-correct files generated by `ggen` (from `generated/mech_factory_mud/ue4/DataTables` and `generated/mech_factory_mud/ue4/Headers`) to the target directories, and save `ProjectionManifest.json` and `ReceiptManifest.json` to the target folder.
   - `falsify`: Programmatically simulate the 8 falsification scenarios in Rust, verify they fail/are refused as expected (by throwing a verification error or being refused), and generate the JSON and MD reports to `generated/mech_factory_mud/falsification/`.
   - `counterfactual`: Programmatically simulate the 8 counterfactual scenarios in Rust, check their expected status/effects, and generate the JSON and MD reports to `generated/mech_factory_mud/counterfactuals/`.

2. **Implement genuine unit/integration tests** to replace all empty or dummy tests in `crates/mech_factory_mud`:
   - In `crates/mech_factory_mud/src/generated_tests.rs`, implement actual logic for:
     - `generated_walkthrough_route_is_connected`: Check that the route node IDs listed in `WalkthroughRoute.csv` form a connected graph matching the route definition.
     - `generated_walkthrough_route_order_is_deterministic`: Check that the route node order is 1 to 9.
     - `generated_projection_commands_have_source_receipt_required`: Check that every row in `ProjectionCommands.csv` has a valid non-empty source receipt field.
     - `generated_header_station_ids_match_factory_stations_csv`: Check that the enum variants in steps/authority header match the IDs in `FactoryStations.csv`.
     - `generated_header_route_ids_match_walkthrough_route_csv`: Check that the route node definitions in header match the CSV.
   - Replace the 24 dummy `assert!(true)` tests in `tests/expanded.rs` and other files with real, functional tests. You can add tests verifying:
     - Authority transitions (heat, damage, stress, next state logic).
     - Scenario simulations and status results.
     - Receipt chain sequence, mutation, and prev_hash verifications.
     - OCEL format verification.
     - Make sure that there are NO empty `assert!(true)` tests or facade/placeholder tests, but all 45+ tests are actual, genuine tests testing real code.

3. **Verify the results**:
   - Run `cargo test --workspace` to ensure all tests pass cleanly.
   - Run `python3 scripts/mud_gap_check.py` to ensure all 11 gap checks pass successfully.
   - Verify `cargo run -p mech_factory_mud -- verify` outputs `PASS`.

4. **Record your changes**, code implementations, tests, commands, and outputs in `handoff.md` in your working directory.
5. Send a message to the parent (conversation ID: 35f1bedd-29d2-4018-9ff0-9132ef45c113) when complete.

MANDATORY INTEGRITY WARNING:
DO NOT CHEAT. All implementations must be genuine. DO NOT
hardcode test results, create dummy/facade implementations, or
circumvent the intended task. A Forensic Auditor will independently
verify your work. Integrity violations WILL be detected and your
work WILL be rejected.
