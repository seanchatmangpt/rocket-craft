## 2026-06-19T05:41:36Z
Remediate the `VaRest` plugin build blocker (exit code 25) by integrating `X3D`, `CityGML`, `BOT`, and `GeoSPARQL` in the `eden_server` and `ue4_ontology` packs.

You must do the following:
1. Integrate the minimum required terms from `X3D`, `CityGML`, `BOT`, and `GeoSPARQL` in `/Users/sac/.ggen/packs/eden_server/ontology/pack.ttl` and `/Users/sac/.ggen/packs/eden_server/ontology/instances.ttl` to represent:
   - Semantic LOD levels
   - Render BOMs and mesh/LOD thresholds
   - Spatial topology
   - Walkthrough geometry
2. For each imported term, map it to a `ggen` generation rule in `ggen.toml` that consumes it to generate static C++ headers, DataTables, or JSON manifests.
3. Remove the `VaRest` plugin requirement from `/Users/sac/rocket-craft/versions/4.27.0/Brm.uproject` (or similar project descriptor files) so it no longer appears in build dependencies.
4. Ensure all runtime Blueprint dynamic HTTP/data-loading paths are disabled/removed, replacing them entirely with static compiled C++/DataTables generated by `ggen`.
5. Run the build/cook pipeline (e.g. `./verify_html5_pipeline.sh` or RunUAT.sh) to verify:
   - `VaRest` is absent from build dependencies.
   - The WebGL build packages successfully.
   - The walkthrough still loads and generates the receipt with PASS verdict.
6. Verify that the output files target valid consumer subdirectories (like `src/`), NOT the pack root, and contain no "DO NOT EDIT" banners.

MANDATORY INTEGRITY WARNING:
DO NOT CHEAT. All implementations must be genuine. DO NOT hardcode test results, create dummy/facade implementations, or circumvent the intended task. A Forensic Auditor will independently verify your work. Integrity violations WILL be detected and your work WILL be rejected.

Your working directory is `/Users/sac/rocket-craft/.agents/worker_remedy_varest/`. Your identity is worker_remedy_varest.
Send a message back to the orchestrator when you are finished, specifying the modifications and build results.

## 2026-06-19T05:42:30Z
**Context**: VaRest remediation and 3D ontologies integration guidelines.
**Content**: URGENT DOCTRINE DIRECTIVE FROM ARCHITECT:
Do not treat `X3D`, `CityGML`, `BOT`, and `GeoSPARQL` as rendering ontologies. They are strictly **world structure ontologies** (semantics, topology, relationships) describing world structure, not meshes, textures, or shaders.
You must map these terms to:
- authority generation
- walkthrough generation
- navigation generation
- topology generation
- render BOM generation
- LOD policy generation
- physics classification
- market classification
- replay generation
This ontology should primarily generate static C++ constants (LOD policies) and navigation graphs to replace the `VaRest` runtime dynamic loading logic. Only generate render assets if a `ggen` template explicitly requires them.
**Action**: Align your ontology structures, queries, and generated files with this world structure definition immediately. Ensure VaRest is removed and the cook/build succeeds.

## 2026-06-19T05:46:04Z
**Context**: Verification of the Projection Law during VaRest removal.
**Content**: DOCTRINE ENFORCEMENT:
"We own the authority. Unreal owns the pixels."
Do not generate WebGL assets from the ontology. Unreal generates and packages render assets. `ggen` must generate semantic authority artifacts consumed by Unreal C++: headers, enums, structs, constants, DataTables, Render BOM metadata, walkthrough coordinates, byte-class matrices, and receipt paths.
`VaRest` is removed only when UE4 no longer requires runtime REST/JSON/Blueprint plugin logic to obtain world structure or Semantic LOD state. The world structure must be statically baked into the C++ compilation.
**Action**: Ensure that all code generation outputs are strictly semantic authority artifacts (headers, enums, structs, constants, DataTables, Render BOM metadata, walkthrough coordinates, byte-class matrices, and receipt paths) consumed by Unreal C++, and that the runtime REST/JSON/Blueprint loading path is completely removed.
